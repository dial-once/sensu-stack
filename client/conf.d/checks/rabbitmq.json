{
  "checks":{
    "rabbitmq-alive":{
      "command":"/opt/sensu/embedded/bin/check-rabbitmq-alive.rb --host {{ .Env.TUTUM_NODE_FQDN }} --port {{ .Env.AMQP_MANAGEMENT_PORT }} --user admin --password {{ .Env.AMQP_MANAGEMENT_PASS }}",
      "standalone":true,
      "occurrences":2,
      "publish":true,
      "interval":10
    }
  }
}
